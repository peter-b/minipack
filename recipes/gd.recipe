name=gd
version=2.0.35
release=1
download=http://www.libgd.org/releases
configure_opt='--disable-static --enable-shared '

src_prep()
{
  autoreconf -ivf
}

# Compatibility with existing binary package
src_install()
{
  do_install
  ln -f $resultdir/lib/libgd.dll.a $resultdir/lib/bgd.lib
}

